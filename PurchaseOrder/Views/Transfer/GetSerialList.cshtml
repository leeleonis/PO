@model List<KeyValuePair< string, string>>
@{
    Layout = null;
}
<table class="table">
    <thead>
        <tr>
            <th width="100px">
                Serial
            </th>
            <th>
                貨況
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Key
                </td>
                <td>
                    @item.Value
                </td>
            </tr>
        }
    </tbody>
</table>

